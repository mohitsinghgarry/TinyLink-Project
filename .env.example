# Example environment variables for TinyLink
# Replace with your actual connection strings when deploying

# Vercel Postgres connection (used on Vercel)
VERCEL_POSTGRES_URL=postgresql://user:password@host:5432/dbname

# Common alternative name for a Postgres connection string
DATABASE_URL=postgresql://user:password@host:5432/dbname

# Base URL of your deployed app (used for UI links)
NEXT_PUBLIC_BASE_URL=https://your-app.vercel.app

# (Optional) Any other environment variables you need
# Example: NEXT_PUBLIC_SENTRY_DSN=
# Database
POSTGRES_URL="postgresql://username:password@host:port/database"

# App
NEXT_PUBLIC_BASE_URL="http://localhost:3000"